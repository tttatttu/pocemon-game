import React from 'react';

const GamePage = () => {

    const handleClick = () => {
        
    }
    return (
        <button onClick={handleClick} >Back</button>
    )
}

export default GamePage
